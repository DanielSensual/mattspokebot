# Matt's Pokemon Center Bot — Railway Configuration
# Worker service (no public port, always-on daemon)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
